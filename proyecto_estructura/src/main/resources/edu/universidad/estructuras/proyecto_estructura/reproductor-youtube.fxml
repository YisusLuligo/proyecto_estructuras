<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.web.WebView?>

<BorderPane xmlns="http://javafx.com/javafx/17"
            xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="edu.universidad.estructuras.proyecto_estructura.controller.ReproductorYoutubeController"
            style="-fx-background-color: #f5f5f5;">

    <!-- Barra Superior -->
    <top>
        <VBox style="-fx-background-color: linear-gradient(to right, #FF0000 0%, #CC0000 100%); -fx-padding: 15;" spacing="5">
            <HBox alignment="CENTER_LEFT" spacing="10">
                <Label text="▶️ YouTube Player" style="-fx-text-fill: white; -fx-font-size: 20; -fx-font-weight: bold;"/>
                <Region HBox.hgrow="ALWAYS"/>
                <Button fx:id="btnCerrar" text="✖ Cerrar" onAction="#handleCerrar"
                        style="-fx-background-color: rgba(255,255,255,0.2); -fx-text-fill: white; -fx-background-radius: 5; -fx-cursor: hand;"/>
            </HBox>
            
            <Separator style="-fx-background-color: rgba(255,255,255,0.3);"/>
            
            <VBox spacing="3">
                <Label fx:id="lblTitulo" text="Título de la canción" 
                       style="-fx-text-fill: white; -fx-font-size: 16; -fx-font-weight: bold;"/>
                <Label fx:id="lblArtista" text="Artista" 
                       style="-fx-text-fill: #ffcccc; -fx-font-size: 14;"/>
            </VBox>
        </VBox>
    </top>

    <!-- WebView Central -->
    <center>
        <StackPane style="-fx-background-color: black; -fx-padding: 10;">
            <WebView fx:id="webView" prefWidth="880" prefHeight="600"/>
        </StackPane>
    </center>

    <!-- Barra Inferior con Botones -->
    <bottom>
        <HBox alignment="CENTER" spacing="15" style="-fx-padding: 15; -fx-background-color: white;">
            <Button fx:id="btnAbrirNavegador" text="🌐 Abrir en Navegador" onAction="#handleAbrirNavegador"
                    style="-fx-background-color: #2196F3; -fx-text-fill: white; -fx-pref-height: 40; -fx-pref-width: 200; -fx-background-radius: 5; -fx-cursor: hand; -fx-font-size: 13;"/>
            
            <Button fx:id="btnAgregarFavoritos" text="❤️ Agregar a Favoritos" onAction="#handleAgregarFavoritos"
                    style="-fx-background-color: #E91E63; -fx-text-fill: white; -fx-pref-height: 40; -fx-pref-width: 200; -fx-background-radius: 5; -fx-cursor: hand; -fx-font-size: 13;"/>
        </HBox>
    </bottom>

</BorderPane>
