<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<BorderPane xmlns="http://javafx.com/javafx/17"
            xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="edu.universidad.estructuras.proyecto_estructura.controller.GestionUsuariosController"
            style="-fx-background-color: #191414;">

    <!-- Barra Superior -->
    <top>
        <HBox alignment="CENTER_LEFT" spacing="20"
              style="-fx-background-color: #000000; -fx-padding: 20;">

            <Button fx:id="btnVolver" text="← Volver" onAction="#handleVolver"
                    style="-fx-background-color: transparent; -fx-text-fill: #B3B3B3; -fx-border-color: #535353; -fx-border-width: 1; -fx-border-radius: 20; -fx-background-radius: 20; -fx-cursor: hand; -fx-font-size: 13; -fx-padding: 8 20 8 20;"/>

            <Region HBox.hgrow="ALWAYS"/>

            <Label text="Gestión de Usuarios" style="-fx-text-fill: white; -fx-font-size: 28; -fx-font-weight: bold;"/>

            <Region HBox.hgrow="ALWAYS"/>

            <Label fx:id="lblTotalUsuarios" text="Total: 0" style="-fx-text-fill: #B3B3B3; -fx-font-size: 14;"/>
        </HBox>
    </top>

    <!-- Panel Central -->
    <center>
        <VBox spacing="20" style="-fx-padding: 30;">

            <!-- Barra de Herramientas -->
            <HBox spacing="12" alignment="CENTER_LEFT">
                <TextField fx:id="txtBuscar" promptText="Buscar por username o nombre..." 
                           style="-fx-background-color: #282828; -fx-text-fill: white; -fx-prompt-text-fill: #B3B3B3; -fx-pref-width: 400; -fx-pref-height: 40; -fx-background-radius: 20; -fx-border-radius: 20; -fx-border-color: transparent; -fx-padding: 0 20 0 20;"/>
                <Button fx:id="btnBuscar" text="🔍 Buscar" onAction="#handleBuscar"
                        style="-fx-background-color: #1DB954; -fx-text-fill: #000000; -fx-pref-height: 40; -fx-background-radius: 20; -fx-cursor: hand; -fx-font-weight: bold; -fx-padding: 0 20 0 20;"/>
                <Button fx:id="btnRefrescar" text="🔄 Refrescar" onAction="#handleRefrescar"
                        style="-fx-background-color: transparent; -fx-text-fill: #B3B3B3; -fx-border-color: #535353; -fx-border-width: 1; -fx-pref-height: 40; -fx-background-radius: 20; -fx-border-radius: 20; -fx-cursor: hand; -fx-padding: 0 20 0 20;"/>
            </HBox>

            <!-- Tabla de Usuarios -->
            <TableView fx:id="tableUsuarios" 
                       style="-fx-background-color: #282828; -fx-control-inner-background: #121212; -fx-background-radius: 8; -fx-table-cell-border-color: #282828;" 
                       VBox.vgrow="ALWAYS">
                <columns>
                    <TableColumn fx:id="colUsername" text="Username" prefWidth="200"/>
                    <TableColumn fx:id="colNombre" text="Nombre" prefWidth="250"/>
                    <TableColumn fx:id="colTipo" text="Tipo" prefWidth="150"/>
                    <TableColumn fx:id="colFavoritos" text="Favoritos" prefWidth="150" style="-fx-alignment: CENTER;"/>
                </columns>
            </TableView>

            <!-- Botones de Acción -->
            <HBox spacing="12" alignment="CENTER">
                <Button fx:id="btnEliminar" text="🗑️ Eliminar Usuario" onAction="#handleEliminar"
                        style="-fx-background-color: transparent; -fx-text-fill: #E22134; -fx-border-color: #E22134; -fx-border-width: 2; -fx-pref-height: 45; -fx-pref-width: 170; -fx-background-radius: 22; -fx-border-radius: 22; -fx-cursor: hand; -fx-font-weight: bold;"/>
            </HBox>

            <!-- Nota -->
            <Label text="⚠️ Nota: No se puede eliminar administradores ni el usuario actual"
                   style="-fx-text-fill: #FFA500; -fx-font-size: 12; -fx-font-style: italic;"/>

        </VBox>
    </center>

</BorderPane>
