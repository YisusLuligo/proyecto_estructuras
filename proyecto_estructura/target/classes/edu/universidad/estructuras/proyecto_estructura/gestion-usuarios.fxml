<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<BorderPane xmlns="http://javafx.com/javafx/17"
            xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="edu.universidad.estructuras.proyecto_estructura.controller.GestionUsuariosController"
            style="-fx-background-color: #f5f5f5;">

    <!-- Barra Superior -->
    <top>
        <HBox alignment="CENTER_LEFT" spacing="20"
              style="-fx-background-color: linear-gradient(to right, #667eea 0%, #764ba2 100%); -fx-padding: 15;">

            <Button fx:id="btnVolver" text="← Volver" onAction="#handleVolver"
                    style="-fx-background-color: rgba(255,255,255,0.2); -fx-text-fill: white; -fx-background-radius: 5; -fx-cursor: hand;"/>

            <Region HBox.hgrow="ALWAYS"/>

            <Label text="👥 Gestión de Usuarios" style="-fx-text-fill: white; -fx-font-size: 24; -fx-font-weight: bold;"/>

            <Region HBox.hgrow="ALWAYS"/>

            <Label fx:id="lblTotalUsuarios" text="Total: 0" style="-fx-text-fill: white; -fx-font-size: 14;"/>
        </HBox>
    </top>

    <!-- Panel Central -->
    <center>
        <VBox spacing="15" style="-fx-padding: 20;">

            <!-- Barra de Herramientas -->
            <HBox spacing="10" alignment="CENTER_LEFT">
                <TextField fx:id="txtBuscar" promptText="Buscar por username o nombre..." style="-fx-pref-width: 300; -fx-pref-height: 35;"/>
                <Button fx:id="btnBuscar" text="🔍 Buscar" onAction="#handleBuscar"
                        style="-fx-background-color: #667eea; -fx-text-fill: white; -fx-pref-height: 35; -fx-background-radius: 5; -fx-cursor: hand;"/>
                <Button fx:id="btnRefrescar" text="🔄 Refrescar" onAction="#handleRefrescar"
                        style="-fx-background-color: #9E9E9E; -fx-text-fill: white; -fx-pref-height: 35; -fx-background-radius: 5; -fx-cursor: hand;"/>
            </HBox>

            <!-- Tabla de Usuarios -->
            <TableView fx:id="tableUsuarios" style="-fx-background-color: white; -fx-background-radius: 5;" VBox.vgrow="ALWAYS">
                <columns>
                    <TableColumn fx:id="colUsername" text="Username" prefWidth="200"/>
                    <TableColumn fx:id="colNombre" text="Nombre" prefWidth="250"/>
                    <TableColumn fx:id="colTipo" text="Tipo" prefWidth="150"/>
                    <TableColumn fx:id="colFavoritos" text="Favoritos" prefWidth="150" style="-fx-alignment: CENTER;"/>
                </columns>
            </TableView>

            <!-- Botones de Acción -->
            <HBox spacing="10" alignment="CENTER">
                <Button fx:id="btnEliminar" text="🗑️ Eliminar Usuario" onAction="#handleEliminar"
                        style="-fx-background-color: #F44336; -fx-text-fill: white; -fx-pref-height: 35; -fx-pref-width: 150; -fx-background-radius: 5; -fx-cursor: hand;"/>
            </HBox>

            <!-- Nota -->
            <Label text="⚠️ Nota: No se puede eliminar administradores ni el usuario actual"
                   style="-fx-text-fill: #e67e22; -fx-font-size: 11; -fx-font-style: italic;"/>

        </VBox>
    </center>

</BorderPane>
